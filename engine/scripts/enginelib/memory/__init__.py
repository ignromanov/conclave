"""enginelib.memory — advisor memory package.

Submodules:
  index   — rebuild advisors/INDEX.md (ported from memory-index.sh)
  hot     — hot.md init (hot-md-init.sh) and append (hot-md-append.sh)
"""
