# Decision — No Frontmatter

This file has no YAML frontmatter at all. Body only.

It was created before the schema_version/type fields were introduced.

**Decision**: Use lib/paths.sh for all path constants.

**Rationale**: Avoids hardcoded strings across scripts.
