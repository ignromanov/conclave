# Baseline capture: /team.start kai-cto
#
# Capture method: raw SKILL.md content (cat of team.start/SKILL.md).
# A clean-session invocation of /team.start kai-cto is not reproducible
# deterministically from a subagent context; the Skill tool is not available
# to exec.atlas-dev. The raw SKILL.md content IS the complete prompt text
# that gets injected when an advisor invokes /team.start — it is therefore
# the correct pre-refactor baseline anchor for B9/B10 ("zero user-visible diff").
#
# Captured at: 2026-05-16
# Commit SHA at capture time: 92502283027fcf9d46ba4e4a31264a0e2e220094
# Branch: master

## raw SKILL.md

---
name: team.start
description: |
  MANDATORY session initialization for ALL advisors (with or without Quorum).
  Loads context, checks resume state, detects task tier, routes to required skills.
  Every advisor session MUST begin with this skill. No exceptions.
---

@../team.forge/contracts/agent-data-policy.md
@../team.forge/contracts/github-issues-protocol.md
@../team.forge/contracts/session-lifecycle.md
@../team.forge/contracts/first-launch-protocol.md
@../team.forge/contracts/decision-framework.md
@../team.forge/contracts/quality-loop.md
@../team.forge/contracts/advisor-anti-patterns.md
@../team.forge/contracts/feedback-protocol.md

# /team.start — Session Initialization

> **MANDATORY** for every advisor session. Works independently — no Quorum required.
>
> **Cardinal Rule**: GitHub Issues = source of truth for tasks. BRIEFING.md = dashboard cache with references (AI#N, GH#N). When they conflict, GH wins.
>
> **Feedback Rule**: if you hit any infra defect during this skill (script bug, naming drift, doc gap), call `.claude/skills/team.forge/scripts/report-issue.sh` immediately. See `feedback-protocol.md`.

## Process

### 1. Load briefing

1. Detect advisor from invocation (`/team.<name>`).
2. Regenerate briefing:
   ```bash
   bash .claude/skills/team.forge/scripts/briefing-build.sh <advisor>
   ```
3. Read `.ai/agent-memory/advisors/briefings/<advisor>.md` into context.
4. The briefing is auto-generated; do not edit.

### 1b. Resume Check

Look for interrupted work from a previous session:

```bash
# Check handoff locations (run in parallel)
ls ops/specs/*/resume-prompt.md 2>/dev/null &
ls ops/handoffs/*-<advisor>-*.md 2>/dev/null &
wait
```

Also inspect the last 3 sessions for this advisor in `.ai/agent-memory/advisors/sessions/` (filter by `frontmatter.advisor`) to detect any session whose status indicates incomplete work.

If found → present to user:
> "Found interrupted work: **[title]** ([path]). Resume or start new?"

If user says resume → read resume-prompt, load required skills from it, skip to Step 5.

### 2. Tier Detection

Classify user request by scale:

| Signal | Tier | Ceremony |
|--------|------|----------|
| Quick question, opinion, <30 min | **Quick** | Minimal GH check (Step 3a), skip steps 4-5 |
| Feature work, 1-4 hours, clear scope | **Feature** | Full init (Steps 3b-6) |
| Multi-session, worktree, epic scope | **Epic** | Full init + checkpoint plan |

### 3. GH Issue Check (MANDATORY — all tiers)

> **GH Issues = source of truth.** Always check before working.
> Both repos feed into a single **VoidPay Roadmap** project board (#3).

#### 3a. Quick Tier (minimal)

```bash
# Count open issues across both repos for this advisor
gh issue list -R ignromanov/voidpay-ai --label advisor:$(ADVISOR_NAME) --state open --json number,title --jq 'length' &
gh issue list -R ignromanov/voidpay --label advisor:$(ADVISOR_NAME) --state open --json number,title --jq 'length' &
wait
```

Show: "You have N open issues (AI: X, Code: Y). P0 blockers: [list or none]."
If user's question relates to an open issue — mention it. Then answer directly.

#### 3b. Feature/Epic Tier (full)

```bash
# Full list from both repos
gh issue list -R ignromanov/voidpay-ai --label advisor:$(ADVISOR_NAME) --state open &
gh issue list -R ignromanov/voidpay --label advisor:$(ADVISOR_NAME) --state open &
# Also check P0 blockers not assigned to this advisor
gh issue list -R ignromanov/voidpay --label p0 --state open &
wait
```

Present open items in a compact table with source repo prefix (AI#N / GH#N).
**Match user's request to an existing issue.** If matched → reference it throughout the session.

**Alternative** — single Project Board query (shows both repos + all custom fields):
```bash
gh project item-list 3 --owner ignromanov --format json --limit 100 | \
  python3 -c "
import json, sys
items = json.load(sys.stdin)['items']
for i in items:
  if i.get('status') != 'Done' and '$(ADVISOR_NAME)' in str(i.get('advisor','')).lower():
    repo = i.get('content',{}).get('repository','')
    num = i.get('content',{}).get('number','')
    title = i.get('content',{}).get('title','')[:60]
    status = i.get('status','')
    print(f'{repo}#{num} [{status}] {title}')
"
```

#### 3c. BRIEFING ↔ GH Reconciliation (Feature/Epic)

Compare briefing Action Items (from the regenerated `.ai/agent-memory/advisors/briefings/<advisor>.md`) with `gh issue list` results from **both repos**:

| Mismatch | Action |
|----------|--------|
| Briefing has AI#N / GH#N but issue is closed | Flag: "AI#N closed in GH but still in briefing — will clean up in /team.done" |
| GH has open issue for this advisor not in briefing | Flag: "AI#N / GH#N exists in GH but missing from briefing" |
| Briefing status differs from Project Board status | Flag drift, use GH as truth |

Present mismatches (if any) before proceeding. Don't fix now — `/team.done` handles cleanup.

### 4. Startup Audit (Feature/Epic only)

```bash
gh pr list --state open &
git branch --no-merged develop &
ls worktrees/ 2>/dev/null &
(cd .ai && git status --short) &
wait
```

Show results as a compact table. Flag mismatches (worktrees without PRs, branches without worktrees). Skip the table if everything is clean.

### 4.5. Wiki Domain Context (Feature/Epic only)

Load domain context from wiki for richer advisory:

| Advisor | Command |
|---------|---------|
| Kai | `/wiki:browse architecture` |
| Shade | `/wiki:browse architecture` + `/wiki:browse concepts` |
| Nexus | `/wiki:browse strategy` |
| Spark | `/wiki:browse strategy` + `/wiki:browse comparisons` |
| Vox | `/wiki:browse orgs` + `/wiki:browse strategy` |
| Dev | skip (architecture loaded via @import) |
| Quorum | skip (all BRIEFINGs loaded via @import) |

Optional: `/wiki:query "topic"` for task-specific context.

### 5. Skill Routing

Detect task type → load required skill chain:

| Task Type | Skill Chain |
|-----------|-------------|
| New feature | brainstorming → writing-plans → workflow.dev-lifecycle |
| Bug fix | systematic-debugging → workflow.dev-lifecycle |
| Content | product-marketing-context → copywriting → copy-editing |
| Grant | grant-proposal-assistant → doc-coauthoring |
| Code review | workflow.pr-review |
| Security | senior-security |
| Meeting | team.quorum |
| Plan execution | subagent-driven-development |

If no matching skill exists:
1. Search with `find-skills`
2. If found installed → invoke
3. If found remote → recommend install to user
4. If not found → work best-effort, log gap in /team.done

### 6. Create Session Tasks (Feature/Epic only)

**MANDATORY**: Create tasks via TaskCreate for the current work session. Always include `/team.done` as the final task so it is never forgotten.

Example:
```
TaskCreate: "Load context and check resume state"       → in_progress
TaskCreate: "[Main work description from user request]" → pending
TaskCreate: "Run /team.done completion checklist"       → pending
```

The `/team.done` task MUST be the last task. It ensures: commits, GH issues sync, BRIEFING update, wiki capture, and handoff if incomplete.

### 7. Present & Confirm (Feature/Epic only)

Show:
- Current focus (from BRIEFING)
- Interrupted work (if any)
- Open issues (if applicable)
- Proposed skill chain
- Created tasks (including /team.done reminder)

Ask: "Proceed with this approach?" (AskUserQuestion)

## Overlay loading

After loading base contracts, scan for per-advisor overlays:

```bash
ADVISOR_ID=$(current_advisor)  # e.g., kai-cto
for base in .claude/skills/team.forge/contracts/*.md; do
  name=$(basename "$base")
  overlay=".claude/skills/team.${ADVISOR_ID}/contracts/${name}"
  [[ -f "$overlay" ]] && echo "apply overlay: $overlay"
done
```

Overlay semantics (`constraint` / `extension` / `replacement`) per
`team.forge/references/aspects/contract-overlays.md`.

## rendered Skill invocation output (if available)

NOT AVAILABLE — exec.atlas-dev subagent context does not have access to the
Skill tool. The raw SKILL.md above is the complete prompt content that an
advisor model receives when /team.start is invoked. For kai-cto specifically,
the overlay scan above would also load any files present at:
  .claude/skills/team.kai-cto/contracts/*.md
(checked at runtime — not statically capturable here).
