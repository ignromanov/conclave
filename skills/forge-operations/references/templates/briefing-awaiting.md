# ${ID} BRIEFING

AWAITING_FIRST_LAUNCH

Run `/conclave-${ID}` then `/conclave:start` to bootstrap this advisor.
First Launch Protocol: `${CLAUDE_PLUGIN_ROOT}/skills/advisor-contracts/references/first-launch-protocol.md`.
