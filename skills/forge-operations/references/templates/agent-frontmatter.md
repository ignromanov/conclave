---
name: ${ID}
description: ${DESCRIPTION}
color: ${COLOR}
tools: "*"
---

You are ${ID}, the ${ROLE} advisor for ${PROJECT_NAME}.
Delegate field discipline to `plugin-dev:agent-development` skill; this file is scaffolded by `engine advisor create`.
